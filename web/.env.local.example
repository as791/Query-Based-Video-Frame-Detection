# Public-facing API URL used by the browser (client-side fetches + OAuth redirect)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Internal API URL used by SSR (server-side fetches inside Docker network)
# In Docker Compose this should be http://api:8080
# API_URL=http://localhost:8080
