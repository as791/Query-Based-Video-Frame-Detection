repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
        name: gitleaks (block secrets before commit)
        args: ["protect", "--staged", "--redact", "--config=.gitleaks.toml"]
